{
  "installCommand": "npm install --legacy-peer-deps && npm install mapbox-gl @mapbox/mapbox-gl-geocoder",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    }
  ]
}